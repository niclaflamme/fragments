[build]
builder = "RAILPACK"
watchPatterns = ["src/**", "posts/**", "assets/**", "Cargo.toml", "Cargo.lock"]

[deploy]
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/healthz"
healthcheckTimeout = 60
